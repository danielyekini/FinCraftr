@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Check for C++20 support
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# Include the targets file
include("${CMAKE_CURRENT_LIST_DIR}/fincraftrTargets.cmake")

# Set up the main target based on what was built
if(TARGET fincraftr::shared)
    add_library(fincraftr::fincraftr ALIAS fincraftr::shared)
elseif(TARGET fincraftr::static)
    add_library(fincraftr::fincraftr ALIAS fincraftr::static)
else()
    add_library(fincraftr::fincraftr ALIAS fincraftr::headers)
endif()

check_required_components(fincraftr)
